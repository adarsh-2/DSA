// A function is called tail recursive if the parent function has nothing to do after the child has finished its execution.

// A function is called tail recursive if the last thing happening in the function is tail recursion.